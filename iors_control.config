# Changing the bitrate changes the rate of the TNC.
bit_rate=1200
serial_device=/dev/ttyUSB0

# Callsigns
callsign=VE2TCP-12

# If a lot of frames are queued by the tnc then broadcast confirms can
# feel slugish.  Keep this value low.
max_frames_in_tx_buffer=2

